# Author: @shailygupta
# consecutive_strings


def consecutive_strings(inputs):
    pass


if __name__ == "__main__":
    print(consecutive_strings(""))
    print(consecutive_strings(""))
    print(consecutive_strings(""))

    # Test Cases
    assert consecutive_strings("") == "", "Should be ___"
    assert consecutive_strings("") == "", "Should be ___"
    assert consecutive_strings("") == "", "Should be ___"
